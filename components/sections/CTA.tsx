"use client";

// ── CTA Section ───────────────────────────────────────────────────────────────
// id="contact"
// Full-width, center-aligned, dark gradient bg (#0d1117 → #070910)
// Radial glow decoration (blue + violet, 15% opacity)
// Badge → H2 gradient text → subtext → gradient CTA button (pulse-glow) → email link

export function CTA() {
  // TODO: Implement with:
  // - Badge pill "Ready to automate?"
  // - H2 "Let's Build Something That Scales." (gradient text, large)
  // - Subtext (DM Sans 400 text-secondary)
  // - "Start a Project →" button (gradient fill, 56px, rounded-full, animate-pulse-glow)
  // - "or email us at hello@noops.studio" (DM Mono 13px text-muted)
  // - scale(1.03) hover on button
  return null;
}
